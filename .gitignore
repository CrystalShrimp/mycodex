# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment & secrets
.env
.env.*
!.env.example
api.txt

# Legacy provider profile files (architecture removed — models now come from
# the machine's codex login). Keep ignored defensively in case old files linger.
config/settings_*.json

# Local/personal config (machine-specific paths, runtime state, generated)
config/shortcuts.json
config/claude_settings.json
config/active_profile

# Logs & runtime output
*.log
logs/
/fed_rate_*.html

# Session persistence
.sessions/

# Windows Zone.Identifier
*.Zone.Identifier

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# User personal notes (report.md is customer-facing — keep trackable)
doc/*
!doc/report.md

# Debug
openclaw_debug/

# Legacy feishu_bot automation artifacts — contain live session tokens,
# browser profile (cookies/login state) and recorded HTTP traffic with secrets.
# The whole toolchain is deprecated; only MANUAL_SETUP.md and openclaw-scopes.json are useful.
scripts/feishu_bot/chrome_profile/
scripts/feishu_bot/recording/
scripts/feishu_bot/artifacts/
# Deprecated feishu_bot automation leftovers (per CLAUDEME.md: only
# MANUAL_SETUP.md and openclaw-scopes.json are still used) — internal only
scripts/feishu_bot/recorder.py
scripts/feishu_bot/replayer.py
scripts/feishu_bot/diagnose_session.py
scripts/feishu_bot/HANDOFF.md
scripts/feishu_bot/help.md
# Feishu setup runtime artifacts and credentials
auto_feishu/feishu-app-result.json
auto_feishu/artifacts/
auto_feishu/node_modules/
prev_work/feishu-app-result.json
prev_work/artifacts/

# Runtime preferences & test cache
.preferences/
.pytest_cache/
.ruff_cache/

# Local runtime workspace config & logs
.claude/
.agents/
audit.log

# Subproject secrets & txt files
auto_feishu/*.txt

