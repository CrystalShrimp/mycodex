# MyCodex 配置与部署规范 (setup.md)

本文档确认 MyCodex 的全局核心配置要素、初始确认方式及多项目免配置切换机制。

---

## 核心架构原则：三大配置要素全局化

为了让开发与协作体验极简（KISS 原则），MyCodex 将**模型 (Model)**、**推理强度 (Effort)**、**审批模式 (Mode)** 三大要素统一为**全局性配置**：

| 要素 | 含义与档位 | 初始确认方式 | IM 动态调整指令 | 全局生效范围 |
|---|---|---|---|---|
| **1. 模型 (Model)** | 调用的 Codex 模型，如：<br>• `gpt-5.6-terra` (旗舰，最强推理)<br>• `gpt-5.6-luna` (均衡推荐)<br>• `gpt-5.5` (轻量快速) | 运行 `codex login` 登录 ChatGPT 账号后本机自动发现。<br>默认使用 `gpt-5.6-terra`，亦可在 `.env` 中配置 `CODEX_DEFAULT_MODEL` | `/model <slug>`<br>例：`/model gpt-5.6-terra` | 全局生效，跨所有目录与会话 |
| **2. 推理强度 (Effort)** | 思维链与推理深度：<br>• `low` (极速响应)<br>• `medium` (推荐，主力均衡)<br>• `high` / `xhigh` / `max` (复杂架构推演) | 系统全局默认 `medium`。<br>可在全局偏好中动态修改 | `/effort low\|medium\|high\|xhigh\|max`<br>例：`/effort medium` | 全局生效，跨所有目录与会话 |
| **3. 审批模式 (Mode)** | 工具调用与敏感操作的拦截控制：<br>• `h` (🛡️ 严格模式：只读沙箱)<br>• `m` (⚖️ 平衡模式：工作区写操作放行)<br>• `l` (⚡ 全自动模式：无沙箱) | 系统全局默认 `m`。<br>在 `.env` 中由 `APPROVAL_MODE=m` 确认 | `/mode h\|m\|l`<br>例：`/mode m` | 全局生效，跨所有目录与会话 |

---

## 切换目录与新建会话体验（彻底废除项目记忆）

1. **删去项目内配置记忆**：
   - 系统不再在工作区内读写 `.mycodex/config.json`；
   - 无论何时切换到哪个项目目录，或者进入从未涉足的全新目录，**均不需要重新配置**，统一自动继承当前全局生效的三大要素。
2. **切换目录仅提示当前参数**：
   - 在聊天软件中执行 `/cd <路径>` 切换目录后，系统不再弹窗或发卡片询问“是否沿用”，而是**仅输出一条提示信息**：
     ```text
     📁 工作区已切换：`D:\projects\demo`
     ⚙️ 当前配置：模型 `GPT-5.6-Terra (旗舰)` | 推理强度 `medium` | 审批模式 `⚖️ 平衡模式 (m)`
     ```
   - 用户可直接发送下一句开发指令（如“给本项目增加登录接口”），Codex 立即按全局配置开始执行。
3. **新会话不重置参数**：
   - 发送 `/new` 重置会话时，只重置会话线程（Codex Thread ID），**保留全部运行配置**，避免重置后重复配置。

---

## 初始部署确认流程（从零到可用）

1. **第一步：Codex 登录**
   - 终端运行 `codex login` 完成 ChatGPT 账号登录（登录凭据保存在 `~/.codex/auth.json`）。
2. **第二步：消息通道确认**
   - 运行 **`setup.cmd`**：
     - `1. 配置飞书 - 个人用`：仅创建者本人可用，不扩大飞书应用可用范围；
     - `2. 配置飞书 - 公用`：将可用范围改为全员，默认 `ALLOWED_USERS` 留空（全员开放），并可一键将特定群聊的所有用户 ID 批量导入为白名单；
     - `3. 飞书群成员一键导入白名单`：日常维护工具，随时批量提取目标群成员并同步到白名单；
     - `4. 配置企业微信`：引导配置智能机器人长连接；
     - `5. 完整配置`：飞书公用 + 企业微信双通道。
3. **第三步：启动服务**
   - 双击 **`MyCodex.bat`** 启动服务（或 `MyCodex-Restart.bat` 重启）。
4. **第四步：直接对话**
   - 在 IM 中直接给机器人发任意任务（如“你好”或开发任务），系统自动按全局配置执行并秒回，零门槛冷启动。
