# MyClaw 安装部署指南（Windows）

> 从零到可用共 4 步，全部双击脚本完成；任何步骤失败都可**直接重跑**，会从上次完成的进度续跑。

## 前置条件

- Windows 10/11，可联网
- 其余（Node.js / Python / Claude Code CLI）缺失时均自动安装，无需提前准备

## 第一步：环境安装（约 10 分钟）

双击根目录 **`MyClaw-Setup.bat`**，按提示确认（直接回车 = 是）：

| 自动完成项 | 说明 |
|---|---|
| Node.js 20+ | 缺失时从国内镜像下载并静默安装 |
| Python 独立环境 `.venv` | 自动安装 uv 并执行 uv sync |
| 飞书自动化依赖 | npm install + Chromium 内核（约 120MB，首次较慢） |
| `.env` 配置文件 | 从模板创建 |
| Claude Code CLI | npm 全局安装（国内镜像） |
| **模型 API Key** | 菜单选 `1` 智谱 / `2` DeepSeek / `3` Kimi → 粘贴 Key → 自动联网验证（HTTP 200 = 通过，401 = Key 有误） |
| 开机自启（可选） | 最后询问，Y = 加入启动项 |

✅ 完成标志：`自检和依赖安装已完成`

## 第二步：飞书机器人配置（约 5-8 分钟）

双击 **`auto_feishu\setup.cmd`**，浏览器自动操作飞书开放平台：

1. **扫码登录**（唯一人工节点：扫码 / 短信验证码）
2. 自动创建或复用应用 → 导入 22+3 项权限 → 启用机器人
3. 自动抓取 App ID / Secret 写入 `.env` → 配置 WebSocket 事件订阅 → 本地服务自动拉起
4. 自动发布应用版本（可用范围全员，权限随版本生效）

✅ 完成标志：`[OK] Feishu setup completed` → 按任意键退出

## 第三步：启动服务

双击 **`MyClaw.bat`**（自动清理残留进程后启动）：

- 验证：浏览器打开 `http://127.0.0.1:8090/health`，返回 `"status":"ok"` 即正常
- 右下角出现托盘图标（首次启动约 30 秒），右键可查看状态 / 日志 / 退出
- 之后重启（改代码或配置后）：双击 `MyClaw-Restart.bat`

## 第四步：添加使用者白名单

1. 用飞书给机器人发任意消息
2. 收到「权限拦截提醒」→ 里面包含使用者的 Open ID（若直接回复则无需此步）
3. 把 Open ID 填入 `.env` 的 `ALLOWED_USERS=`（多人用逗号分隔；**留空 = 允许所有人**）
4. 双击 `MyClaw-Restart.bat` 生效

## 验证与开始使用

- 发一条消息 → 机器人流式回复 ✅
- 首次使用会弹 3 张卡片依次选择：模型供应商 / 档位（haiku 快、sonnet 均衡、opus 强）/ 审批模式（h 严格、m 平衡、l 全自动）
- `/status` 查看运行状态；`/help` 查看全部指令

## 常见问题

| 现象 | 处理 |
|---|---|
| 窗口闪退或中文乱码 | 使用最新版脚本（旧版编码问题已修复）；失败时窗口会停住并显示原因，按提示处理 |
| Chromium 下载慢 / 失败 | 网络问题，重跑即可；或手动执行 `cd auto_feishu` 后 `npx playwright install chromium` |
| 收到「权限拦截提醒」 | 正常引导流程：把消息里的 Open ID 加入 `.env` 的 `ALLOWED_USERS` |
| 服务 60 秒未上线 | 查看 `logs\myclaw.log`，然后双击 `MyClaw-Restart.bat` |
| API Key 无效 / 想换供应商 | 重跑 `MyClaw-Setup.bat`，重新选择供应商并粘贴 Key |
| 机器人不回消息 | 依次检查：托盘是否在 → `/health` 是否 ok → 是否换了飞书应用（换过则需重新执行第二步） |
